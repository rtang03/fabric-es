apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: org2peer1-pv-claim
spec:
  storageClassName: manual
  selector:
    matchLabels:
      name: org2peer1_production
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
