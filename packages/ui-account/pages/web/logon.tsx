import { NextPage } from 'next';
import React from 'react';
import Layout from '../../components/Layout';

const Logon: NextPage<any> = () => {
  return <Layout title="Account | Log on">log on</Layout>;
};

export default Logon;
