export type DidDocumentInRedis = {
  verification: string;
  context: string;
  controller: string;
  created: string;
  id: string;
  keyagr: string;
  proof: string;
  service: string;
  updated: string;
  ts: number;
};
