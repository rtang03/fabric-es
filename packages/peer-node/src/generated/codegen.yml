overwrite: true
schema: "http://localhost:15000/graphql"
generates:
  src/generated/peer-resolvers-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: false
