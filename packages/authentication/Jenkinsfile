pipeline {
    agent {
        docker {
            image 'node:8.17.0-alpine' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'yarn install --ignore-engines'
                sh 'yarn build:authentication'
            }
        }
    }
}