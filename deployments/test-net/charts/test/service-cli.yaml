apiVersion: v1
kind: Service
metadata:
  name: {{ include "orgadmin.fullname" . }}--cli
  labels:
{{ include "labels.standard" . | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 
  selector:
    app: {{ include "orgadmin.name" . }}--cli
    release: {{ .Release.Name }}
