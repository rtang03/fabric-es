{
  "name": "@fabric-es/model-pboc",
  "version": "0.7.5",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "BSD-3-Clause-Clear",
  "author": "Paul Lai",
  "description": "Model - PBoC / eTC connectivity",
  "scripts": {
    "build": "yarn clean && tsc",
    "test": "yarn build && ./node_modules/.bin/jest unit.test",
    "clean": "rimraf dist",
    "lint": "eslint --max-warnings 10 'src/**/*.{js,ts}'"
  },
  "private": true,
  "dependencies": {
    "@fabric-es/fabric-cqrs": "^0.7.5",
    "@fabric-es/gateway-lib": "^0.7.5",
    "@fabric-es/relay-lib": "^0.7.5"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.0",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
