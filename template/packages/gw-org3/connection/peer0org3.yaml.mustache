---
name: {{network.compose-project-name}}_{{network.name}}-{{orgs.peer.2.name}}
version: 1.0.0

client:
  organization: {{orgs.peer.2.name}}
  credentialStore:
    path: ./assets/client-certs/hfc-kvs/{{orgs.peer.2.domain}}
    cryptoStore:
      path: ./assets/client-certs/hfc-kvs/{{orgs.peer.2.domain}}
  connection:
    timeout:
      peer:
        endorser: '300'

channels:
  eventstore:
    orderers:
      - {{orgs.orderer.orderers.endpoints.0.-}}{{network.host-delimiter}}{{orgs.orderer.domain}}
    peers:
      {{#orgs.peer.2.peers.endpoints}}
      {{-}}{{network.host-delimiter}}{{orgs.peer.2.domain}}:
        endorsingPeer: true
        chaincodeQuery: true
        ledgerQuery: true
        eventSource: true
      {{/orgs.peer.2.peers.endpoints}}
    chaincodes:
      - eventstore:1.0

organizations:
  {{orgs.peer.2.id}}:
    mspid: {{orgs.peer.2.id}}
    peers:
      {{#orgs.peer.2.peers.endpoints}}
      - {{-}}{{network.host-delimiter}}{{orgs.peer.2.domain}}
      {{/orgs.peer.2.peers.endpoints}}
    certificateAuthorities:
      - {{orgs.peer.2.ca.host}}{{network.host-delimiter}}{{orgs.peer.2.domain}}
    adminPrivateKey:
      path: ../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}{{{orgs.peer.2.admin-cert-path}}}/msp/keystore/key.pem
    signedCert:
      path: ../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}{{{orgs.peer.2.admin-cert-path}}}/msp/signcerts/cert.pem

orderers:
  {{orgs.orderer.orderers.endpoints.0.-}}{{network.host-delimiter}}{{orgs.orderer.domain}}:
    url: grpcs://localhost:{{orgs.orderer.orderers.endpoints.0.ext-port}}
    grpcOptions:
      ssl-target-name-override: {{orgs.orderer.orderers.endpoints.0.-}}{{network.host-delimiter}}{{orgs.orderer.domain}}
    #      grpc-max-send-message-length: 4000
    tlsCACerts:
      path: ../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}{{{orgs.orderer.orderers.endpoints.0.cert-path}}}/tls-msp/tlscacerts/tls-0-0-0-0-{{orgs.tls.ca.port}}.pem

peers:
  {{#orgs.peer.2.peers.endpoints}}
  {{-}}{{network.host-delimiter}}{{orgs.peer.2.domain}}:
    url: grpcs://localhost:{{port}}
    eventUrl: grpcs://localhost:{{eventhub-port}}
    tlsCACerts:
      path: ../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}{{{cert-path}}}/tls-msp/tlscacerts/tls-0-0-0-0-{{orgs.tls.ca.port}}.pem
    grpcOptions:
      ssl-target-name-override: {{-}}{{network.host-delimiter}}{{orgs.peer.2.domain}}
  {{/orgs.peer.2.peers.endpoints}}

certificateAuthorities:
  {{orgs.peer.2.ca.host}}{{network.host-delimiter}}{{orgs.peer.2.domain}}:
    url: https://localhost:{{orgs.peer.2.ca.port}}
    caName: {{orgs.peer.2.ca.host}}{{network.host-delimiter}}{{orgs.peer.2.domain}}
    tlsCACerts:
      path: ../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}{{{orgs.peer.2.msp-path}}}/tlscacerts/tls-ca-cert.pem
    registrar:
      - enrollId: {{orgs.peer.2.ca.admin}}
        enrollSecret: {{orgs.peer.2.ca.adminpw}}
    httpOptions:
      verify: false
