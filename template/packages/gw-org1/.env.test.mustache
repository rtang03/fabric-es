### Used by fabric-cqrs
#Orgnaization Admin EnrollmentID
ORG_ADMIN_ID=admin-{{orgs.peer.0.domain}}
#Organization Admin EnrollSecret
ORG_ADMIN_SECRET={{orgs.peer.0.name}}AdminPW
#URL for Orgnization CA
ORG_CA_URL=https://0.0.0.0:{{orgs.peer.0.ca.port}}
# org name defined in connnection profile; case sensitive
ORGNAME={{orgs.peer.0.domain}}
# Path to Fabric Network
NETWORK_LOCATION=../../network2/config/{{{artifacts.local}}}/{{{artifacts.crypto}}}{{#artifacts.crypto}}/{{/artifacts.crypto}}.
# Path to Connection Path
CONNECTION_PROFILE=connection/peer0org1.yaml
# Relative path to wallet directory
WALLET=assets/wallet
# Channel Event Hub
CHANNEL_HUB={{orgs.peer.0.peers.endpoints.0.-}}{{network.host-delimiter}}{{orgs.peer.0.domain}}
# Optional:
CHAINCODE_ID=eventstore
CHAINCODE_ID_PRIVATEDATA=privatedata
# Used in logger
APPLICATION={{orgs.peer.0.peers.endpoints.0.-}}{{orgs.peer.0.domain}}app
# Bootstrap
COLLECTION={{orgs.peer.0.name}}PrivateDetails
# ENROLLMENT ID OF ADMIN, used by bootstrap-xxx.ts
ENROLLMENT_ID_ADMIN=admin
# Used by service-admin
CHANNEL_NAME=eventstore
PEER_NAME={{orgs.peer.0.peers.endpoints.0.-}}{{network.host-delimiter}}{{orgs.peer.0.domain}}

# Used by enrollCaAdmin.ts of gw-node/service-admin
CA_ENROLLMENT_ID_ADMIN={{orgs.peer.0.ca.admin}}
CA_ENROLLMENT_SECRET_ADMIN={{orgs.peer.0.ca.adminpw}}
CA_URL=https://0.0.0.0:{{orgs.peer.0.ca.port}}

# Used in gateway.ts of gw-node/service-admin
AUTHORIZATION_SERVER_URI=http://localhost:{{orgs.peer.0.gw.auth.port}}{{{orgs.peer.0.gw.auth.path}}}
ADMINISTRATOR_PORT={{orgs.peer.0.gw.auth.adminport}}

# Used by createModel.ts of packages/authentiction
ACCESS_TOKEN_SECRET=ajlkqowieuqowueoi
REFRESH_TOKEN_SECRET=qopwieioque1
AUTHORIZATION_CODE=kljkiojkl
CORS_ORIGIN=http://localhost:3000

# Used in clientResolver.ts of packages/authentication
# Root_Admin account is used to create other administrator
ADMIN=root_admin
ADMIN_PASSWORD=root_admin_password
# OAUTH_SERVER_PORT is the same as AUTHORIZATION_SERVER_URI
OAUTH_SERVER_PORT={{orgs.peer.0.gw.auth.port}}

# Federated gateway and services
# Used in start-gateway.ts
GATEWAY_HOST={{orgs.peer.0.gw.host}}
GATEWAY_PORT={{orgs.peer.0.gw.port}}
# Used in service-user.ts
SERVICE_USER_PORT={{orgs.peer.0.gw.services.user.port}}
# Used in service-loan.ts
SERVICE_LOAN_PORT={{orgs.peer.0.gw.services.loan.port}}
# Used in service-doc.ts
SERVICE_DOCUMENT_PORT={{orgs.peer.0.gw.services.document.port}}
# Used in service-private.ts
SERVICE_PRIVATE_PORT={{orgs.peer.0.gw.services.private.port}}
# Used in service-remote-data.ts
REMOTE_LOAN_DETAILS_PORT={{orgs.peer.0.gw.services.remote-loan-details.port}}
REMOTE_LOAN_DETAILS_NAME={{orgs.peer.0.gw.services.remote-loan-details.name}}
REMOTE_LOAN_DETAILS_URI={{{orgs.peer.0.gw.services.remote-loan-details.uri}}}

# Used by TypeORM of packages/authentication
TYPEORM_SCHEMA=public
TYPEORM_CONNECTION=postgres
TYPEORM_HOST={{orgs.peer.0.gw.auth.dbhost}}
TYPEORM_USERNAME={{orgs.peer.0.gw.auth.dbuser}}
TYPEORM_PASSWORD={{orgs.peer.0.gw.auth.password}}
TYPEORM_DATABASE={{orgs.peer.0.gw.auth.dbname}}
TYPEORM_PORT={{orgs.peer.0.gw.auth.dbport}}
TYPEORM_SYNCHRONIZE=true
TYPEORM_LOGGING=false
