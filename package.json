{
  "name": "open-platform-dlt",
  "version": "1.0.0",
  "scripts": {
    "cp:root": "rimraf .build && mkdir -p ./.build && cp ./package.json .build && cp yarn.lock .build ",
    "build:authentication": "lerna run build:prod --scope=@espresso/authentication",
    "build:lib": "lerna run build --scope={@espresso/fabric-cqrs,@espresso/operator,@espresso/gw-node}",
    "build:gw-org1": "lerna run build:prod --scope={@espresso/model-common,@espresso/model-loan-private,@espresso/model-loan,@espresso/gw-org1}",
    "build:gw-org2": "lerna run build:prod --scope={@espresso/model-common,@espresso/model-loan-private,@espresso/model-loan,@espresso/gw-org2}",
    "package-auth-only": "yarn run cp:root && lerna run package --scope=@espresso/authentication",
    "package-org1": "yarn run cp:root && lerna run package --scope={@espresso/authentication,@espresso/fabric-cqrs,@espresso/operator,@espresso/gw-node,@espresso/model-common,@espresso/model-loan-private,@espresso/model-loan,@espresso/gw-org1}",
    "package-org2": "yarn run cp:root && lerna run package --scope={@espresso/authentication,@espresso/fabric-cqrs,@espresso/operator,@espresso/gw-node,@espresso/model-common,@espresso/model-loan-private,@espresso/model-loan,@espresso/gw-org2}"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "listr": "^0.14.3",
    "listr-silent-renderer": "^1.1.1",
    "listr-update-renderer": "^0.5.0",
    "listr-verbose-renderer": "^0.6.0",
    "json-to-pretty-yaml": "^1.2.2",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.0",
    "tslint": "^5.20.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.7.2"
  },
  "prettier": {
    "eslintIntegration": false,
    "semi": true,
    "singleQuote": true,
    "tabWidth": 2,
    "bracketSpacing": true,
    "jsxBracketSameLine": true,
    "requirePragma": false,
    "insertPragma": false
  }
}
